{
  "name": "vitalnet-root",
  "version": "1.0.0",
  "description": "VitalNet - Healthcare Emergency Management Platform",
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
